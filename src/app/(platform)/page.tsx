import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'GoodJob',
    description: 'GoodJob',
};

export default function PlatformPage() {
    return <div>플랫폼 메인페이지</div>;
}
